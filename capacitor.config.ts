import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.githubrepobrowser',
  appName: 'github-repo-browser',
  webDir: 'dist/github-repo-browser/browser',
};

export default config;
